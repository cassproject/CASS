ppk=#fileLoad(path="adapter.ceasn.private.pem").fileToString();
ppk=#if(
	operator=#fileExists(path="adapter.ceasn.private.pem"),
	operand="false",
	eq=#object(
		a=#fileSave(obj=#rsaGenerate(),path="adapter.ceasn.private.pem"),
		b=ppk
	).cget(b=""),
	ne=ppk
).cache(name="adapter.ceasn.private.pem");
#ceasnPpk=ppk;
